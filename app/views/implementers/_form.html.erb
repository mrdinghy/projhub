<%= simple_form_for(@implementer) do |f| %>
  <% if @implementer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@implementer.errors.count, "error") %> prohibited this implementer from being saved:</h2>

      <ul>
      <% @implementer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.input :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.input :description, input_html: {rows: 10, cols: 80}, :label => false  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
