<%= form_for(@implementer) do |f| %>
  <% if @implementer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@implementer.errors.count, "error") %> prohibited this implementer from being saved:</h2>

      <ul>
      <% @implementer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
